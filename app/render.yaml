services:
  - type: web
    name: panel-creation-dashboard
    env: node
    plan: free
    buildCommand: npm install && npm run build && cd backend && npm install
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PORT
        value: 10000
